repos:
  - repo: local
    hooks:
      - id: run-tests
        name: Run repository tests
        entry: make test
        language: system
        always_run: true

      - id: shellcheck
        name: shellcheck
        entry: shellcheck
        language: system
        types: [shell]
